# Direct dependencies only. pip resolves the rest.
# This file used to be a full `pip freeze` (102 packages) and pulled in
# torch, transformers, sentence-transformers, scikit-learn and selenium,
# none of which are imported anywhere in the project. They added several
# GB to the Docker image for nothing.

# Web
Flask==3.1.2
Flask-Login==0.6.3
Flask-SQLAlchemy==3.1.1
gunicorn==23.0.0

# Database
psycopg2-binary==2.9.10

# Config
python-dotenv==1.1.1

# AI
google-generativeai==0.8.5

# Resume parsing / OCR
PyPDF2==3.0.1
python-docx==1.2.0
pdf2image==1.17.0
pytesseract==0.3.13
pillow==11.3.0

# Job scraping
requests==2.32.5
beautifulsoup4==4.13.5
